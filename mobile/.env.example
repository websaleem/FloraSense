# Base URL of the FloraSense API, without a trailing slash. The app calls
# $EXPO_PUBLIC_API_BASE_URL/predict. Point it at the CloudFront domain, never the
# raw Lambda Function URL, which only accepts CloudFront-signed requests.
EXPO_PUBLIC_API_BASE_URL=https://dev.florasense.websaleem.com
